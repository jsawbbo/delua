#if defined(DEBUG)

#include <unistd.h>
#include <string.h>
#include <stdio.h>

int __lua_exec_in_buildpath() {
#if defined(_GNU_SOURCE)
	const char *executable_output_path = "@EXECUTABLE_OUTPUT_PATH@";
	return 0 == strncmp(program_invocation_name, executable_output_path, strlen(executable_output_path));
#else
	return 1;
#endif
}

#endif